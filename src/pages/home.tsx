import NavBar from "@/components/NavBar";
import SearchBar from "@/components/SearchBar";

const Home = () => {
  return (
    <main className="home">
      <NavBar />
      <SearchBar />
    </main>
  );
};

export default Home;
